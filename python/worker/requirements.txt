# Pinned to the majors in use (see python/env). Update deliberately;
# unpinned deps previously meant any rebuild could pull breaking majors.
# Discord API
discord.py>=2.6,<3

# Database
tortoise-orm>=0.25,<0.26
asyncpg>=0.30,<0.31

# Redis
redis>=6.4,<7

# Data validation
pydantic>=2.12,<3

# Environment variables
python-dotenv>=1.1,<2

# Logging and utilities
python-json-logger>=4,<5
colorlog>=6.10,<7

# Async file I/O for storage
aiofiles>=25.1,<26

# Video processing (requires ffmpeg binary installed on system)
ffmpeg-python>=0.2,<0.3
Pillow>=11.3,<12

# Private Google Cloud Storage backend and explicit migration command
google-cloud-storage>=2.19,<4

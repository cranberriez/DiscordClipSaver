services:
    bot:
        build:
            context: .
            dockerfile: ./bot/Dockerfile
        container_name: discord-clip-saver-bot
        env_file:
            - ./.env.global
            - ./bot/.env
        ports:
            - "8000:8000"
        depends_on:
            - discord-clip-saver-postgres
            - discord-clip-saver-redis
        restart: unless-stopped
        volumes:
            # Optional: mount local settings file for rapid iteration without rebuilding image
            - ./settings.default.yml:/app/settings.default.yml:ro

    interface:
        build:
            context: .
            dockerfile: ./interface/Dockerfile
        container_name: discord-clip-saver-interface
        env_file:
            - ./.env.global
            - ./interface/.env
        ports:
            - "3000:3000"
        depends_on:
            - bot
            - discord-clip-saver-postgres
            - discord-clip-saver-redis
        restart: unless-stopped
        volumes:
            # Optional: mount local settings file to the same in-container path used by the image
            - ./settings.default.yml:/app/settings.default.yml:ro

    discord-clip-saver-postgres:
        image: postgres:16-alpine
        container_name: discord-clip-saver-postgres
        environment:
            POSTGRES_USER: ${POSTGRES_USER:-discord}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-discord}
            POSTGRES_DB: ${POSTGRES_DB:-discord_clip_saver}
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data
        networks:
            default:
                ipv4_address: 172.20.0.10
        restart: unless-stopped

    discord-clip-saver-redis:
        image: redis:7-alpine
        container_name: discord-clip-saver-redis
        command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data
        networks:
            default:
                ipv4_address: 172.20.0.11
        restart: unless-stopped

networks:
    default:
        name: discord-clip-saver
        driver: bridge
        ipam:
            config:
                - subnet: 172.20.0.0/24

volumes:
    postgres_data:
    redis_data:
